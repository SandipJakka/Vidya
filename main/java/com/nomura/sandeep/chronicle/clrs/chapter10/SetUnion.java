package com.nomura.sandeep.chronicle.clrs.chapter10;

/**
 * 10.2.6
 * Dynamic set operation which takes 2 disjoint sets S1 and S2 and returns union in O(1) using suitable list
 */
public class SetUnion {
    /***
     * In case there are NO common elements then O(1) is possible ...else not possible.
     *
     * Join the head of one linked list to the tail of other.
     */
}
